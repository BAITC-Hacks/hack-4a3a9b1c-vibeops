# Промпт агенту Нурбола

Ты реализуешь интерфейс альтернатив и сравнения Firebird в командном репозитории BAITC-Hacks/hack-4a3a9b1c-vibeops.

Сначала прочитай AGENTS.md, docs/decision-support/SPEC.md, shared/contracts.ts, shared/decision-support.ts, client/src/App.tsx и client/src/api.ts. Проверь git status, сохрани изменения и обновись от актуального main без force/reset. Работай в своей ветке только в client/**, tests/ui/** и docs/ui-notes.md.

Добавь поддержку optional result.decision_support по контракту SPEC. Старый ответ без поля должен продолжать работать. При наличии проверяй форму данных и соответствие предложений исходному query; не вычисляй цены/календарь и не сортируй кандидатов на клиенте.

Добавь «Что можно изменить»: до двух вариантов, одна дата и один бюджет. Показывай серверные title/explanation и количество подходящих. Кнопка применяет полный alternative.query к форме и запускает обычный существующий POST через run, очищает activeDemo; до клика не меняй исходный результат. Сохрани защиту AbortController/sequence от старых ответов.

Добавь «Особенности для сравнения» для 2–3 текущих карточек на основе comparison и имён из cards, в исходном порядке. Источник llm/fallback/catalog не искажать; не выдумывать рейтинги или преимущества. На мобильном не должно быть горизонтального скролла.

Сначала можно сделать unit-fixtures нового контракта; это только тестовые ответы, их нельзя включать в production. После backend Никиты обязательно пройти настоящий API в браузере: пустой/редкий результат → предложение → клик → новый результат; изменение затрагивает только одну настройку, язык и часы остаются. Отдельно проверить отсутствие предложений, отсутствие поля на старом backend, неверный формат ответа, быстрые повторные клики, fallback и ошибки.

Доработай итог существующего tests/ui/acceptance.mjs: отдельно перечисляй проверенные режимы llm/mixed/fallback/not_needed, чтобы общий PASS не воспринимался как доказательство live AI. Не дублировать отдельный серверный live-check Никиты, если он уже добавлен.

Выполни npm run test:ui и npm run build. Сделай commit/push/PR; укажи SHA, тесты и что проверено через настоящий браузер, а что только fixtures. Не менять backend, shared-контракт и корневой package/lock.
