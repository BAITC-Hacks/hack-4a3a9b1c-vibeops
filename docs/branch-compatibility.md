# Совместимость веток и релиза — 23.09.2026

Проверка GitHub и локального Git около 17:27–17:39, UTC+5. Это отчёт о проверочной ветке, не новая публикация релиза и не подтверждение сдачи на платформе.

## Состояние до исправлений

- Все PR #1–#18 объединены; открытых PR в GitHub REST-коллекции не было.
- Опубликованный, не черновой релиз `v0.1.0` и `origin/main`: `33ffb60cc387b872e1044e9ec17d0caf27c2378c`.
- Между проверенным в отчёте релиза `a84cc6b` и `v0.1.0` нет изменений runtime, контракта, зависимостей или CSV: только документы, доказательства приёмки и live-test script.
- GitHub Actions для `main` не содержит запусков; зелёный CI не подменяет локальные результаты ниже.

| Рабочая ветка | Состояние относительно релиза |
| --- | --- |
| `codex/acceptance` | Полностью в истории `main` |
| `codex/ai-concierge` | Полностью в истории `main` |
| `codex/clear-explanations` | Полностью в истории `main` |
| `codex/decision-support-acceptance` | Полностью в истории `main` |
| `codex/decision-support-contract` | Полностью в истории `main` |
| `codex/distinct-evidence-fix` | Полностью в истории `main` |
| `codex/integrate-ui` | Полностью в истории `main` |
| `codex/release-readiness` | Полностью в истории `main` |
| `codex/scaffold` | Полностью в истории `main`, включая альтернативы Никиты |
| `codex/ui-draft-preservation` | Полностью в истории `main`, PR #17 |
| `feat/explanations` | Полностью в истории `main` |
| `feat/ui` (`7ea7c15`) | Разошлась с `main`: 17 коммитов только в main, 5 только в ветке; повторное слепое слияние небезопасно |

Наличие старой ветки на GitHub не означает, что её работа потеряна или не объединена.

Во время проверки в `main` вошёл PR #19 (`codex/fix-brief-followups`): новый `main` — `c7fd69f99c13ad8250a0bc2f3ad6103e43a47c35`. Отличия от релиза — только README и дополнительный тест уточнений. Этот `main` также объединён в проверочную ветку без конфликтов; повторный backend-прогон — 135/135. Таким образом, на момент передачи проверены 13 рабочих веток, 12 уже входят в `main`; все PR #1–#19 объединены. `v0.1.0` остаётся на `33ffb60`, а не перемещается вслед за `main`.

## Что исправлено в `codex/release-compatibility`

Пробный merge `origin/feat/ui` в релиз выявил конфликты в пяти файлах: README, App, Assistant, assistant-brief, assistant-compare.

- Сохранены актуальные UI релиза, применение альтернатив, сброс устаревшего диалога и защита от запоздалых ответов. Итоговые `client/**`, `tests/ui/**`, `shared/**`, зависимости и CSV не изменены относительно релиза.
- Сохранены обработка естественных дат/валют и усиленная проверка цитат релиза; не возвращён старый backend из UI-ветки.
- Перенесены совместимые дополнения: план доступа жюри, дополнительный live-сценарий исправления даты, необязательная настройка Astra и mock-проверка трёх адаптеров с Astra, Sol и 4.1-mini.
- `.env.example` не переключает всех на непроверенную Astra. Изначально сохранён пример релиза; перед объединением учтён новый main `623c114`: команда выбрала в примере `gpt-6-sol`, соответствующую живой приёмке релиза. README и план доступа жюри согласованы с этим изменением. Параметр Astra `reasoning.effort=low` сверен с [OpenAI Docs](https://developers.openai.com/api/docs/models/gpt-6-astra); это не доказательство доступа или скорости в аккаунте команды.
- Старое требование теста «дата без года всегда запрещена» согласовано с реализованным календарным правилом: год вычисляется сервером и показывается предупреждение, а не заимствуется из посторонней фразы.
- Воспроизведена и исправлена реальная ошибка: «11 октября или 12 октября 2026» / «11 октября. Альтернатива: 12 октября 2026» могли дать один выбранный день при укороченной цитате модели. Теперь требуется выбор даты. Добавлен регрессионный тест.

## Фактически выполнено

Изолированный worktree от релиза; Node **24.21.0**, установка зависимостей `npm ci --no-audit --no-fund`.

| Проверка | Результат |
| --- | --- |
| `npm run typecheck` | PASS |
| `npm run build` после исправления | PASS |
| `node --import tsx --test --test-concurrency=1 tests/**/*.test.ts` | **135/135 PASS**, без пропусков, после включения PR #19 |
| `npm run test:ui -- --maxWorkers=1` | **61/61 PASS**, 4 файла |
| `npm start`, отдельный `PORT=4320` | Production запущен |
| `node tests/ui/acceptance.mjs http://127.0.0.1:4320` | D1–D6, по 3 повтора: **18 HTTP-запросов PASS**, 2–89 мс |
| `node --import tsx tests/explanations/decision-live.ts --run http://127.0.0.1:4320` | 7 сценариев альтернатив, **18 HTTP-запросов PASS** |
| SHA-256 CSV | `6a724b6b7dfb5973343e68ba18dadb60fc807d87e3d78f03ee86fb26cb089f7d`, неизменён |

Первый одновременный запуск backend/UI/build исчерпал память Windows; он **не считается успешной проверкой**. Последовательные запуски с одним worker завершились успешно. Offline-установка не нашла Vitest 5.0.1 в кэше; обычный `npm ci` завершился успешно. Это ограничения окружения, не скрытые пропуски тестов.

## Границы проверки и передача

- API-ключа в проверочной копии и переменных процесса нет. HTTP-приёмка выполнена в `fallback/not_needed`, тесты AI — с подставным провайдером/HTTP-ответами. **Настоящий AI после этого исправления не проверен**; прежняя live-приёмка относится к релизу, не к новому SHA.
- В этом проходе не повторялись браузерная визуальная проверка, холодный перезапуск и запуск на втором компьютере. UI не менялся; его автоматические тесты выполнены.
- Исправления передаются отдельным PR. Не перезаписывать `v0.1.0`: перед новым релизом проверить реальный AI на объединённом SHA и настроенной модели, затем зафиксировать новую версию по решению команды.
- Доступ жюри к серверу, второй компьютер и кнопка «Сдать решение» остаются отдельными неподтверждёнными пунктами [чек-листа](RELEASE-CHECKLIST.md).

## Финальная интеграция около 17:51

PR #20 объединён в `main` как `8e23236`; сохранены также изменения документации PR #22 и выбранная командой `gpt-6-sol`. Исправление бюджета словами из PR #21 (`1022629`) объединено с этим main в проверенный `68c9dc2` без конфликтов кода.

На объединённой версии повторно прошли **142/142 backend-теста**, **61/61 UI-тест** и `npm run build` (включая typecheck), последовательно с одним test worker. Установленные зависимости сверены через `npm ls --depth=0`. Это локальные проверки с mock-провайдерами, не новый live-AI отчёт. Результаты HTTP-проверок выше относятся к этапу до добавления сумм словами; не подменяют проверку настоящего AI на итоговом SHA.

Ранее незакоммиченные ТЗ добавлены в Git с пометкой «исторический документ». Исходные локальные копии сохранены в `tmp/committed-audits`; временные материалы и `.env` не публикуются. Тег `v0.1.0` не перезаписывается.
