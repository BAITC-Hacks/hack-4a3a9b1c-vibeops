# Проверка локального запуска — 23.09.2026

Проверяемая версия приложения: `4625919e49d4c17de0f44224d72872489b9c5b5b`. Приёмочные скрипты Нурбола взяты из `18532f6` и проверены в интеграционной ветке. Ниже фактические результаты, а не прогноз готовности финальной сдачи.

## Чистая копия

Создан отдельный Git-клон без старых node_modules/dist и без личного .env. По README выполнены npm ci → копирование .env.example → npm run build → npm test → npm run test:ui. Все прошли: 32 backend/AI-теста и 24 UI-теста. В интеграционной ветке дополнительно прошли 11 самопроверок приёмочного валидатора, итого UI-набор 35.

Production-сервер запущен из чистой сборки. Каталог: 66 профилей. При запуске без ключа health сообщает ai_configured=false; непустые выдачи маркируются fallback. Для live-проверки тот же сервер получил локальную конфигурацию через Node --env-file; секрет не копировался в клон, отчёт или репозиторий.

## Реальные HTTP-проверки

`node tests/ui/acceptance.mjs http://127.0.0.1:4325` выполнен дважды: без ключа и с настоящим OpenAI. Каждый запуск — D1–D6 по три повторения, 18 запросов. Оба завершились PASS. Проверены исходы, допустимые ID, число карточек, null, стартовый бюджет, занятость, пересекающиеся причины, неизменность dataset SHA, порядок и согласованность источников объяснений.

| Проверка | Без AI | С настоящим AI |
|---|---:|---:|
| D1 холодный запрос, отдельный от runner | 28 мс, fallback | 1897 мс, llm |
| D2 первый запрос runner | 5 мс, fallback | 2108 мс, llm |
| D3 первый запрос runner | 4 мс, fallback | 964 мс, llm |
| D4 / D5 | not_needed | not_needed |
| D6 первый запрос runner | 3 мс, fallback | 1727 мс, llm |

Повторы live-запросов использовали кэш; эти задержки не выдаются за скорость модели. Замеры единичные, зависят от сети и нагрузки провайдера.

## Перезапуск

Процесс сервера полностью остановлен и запущен заново. Порядок D1 совпал до перезапуска, после него и при переключении fallback → live:

`HK-88430 → HK-27222 → HK-29829`.

Отдельный автоматический тест также проверяет порядок после перестановки массива и повторной загрузки CSV.

## Что пока нельзя считать закрытым

- Финальный SHA перед дедлайном нужно перепроверить после последующих изменений.
- Чистый клон проверен на ноутбуке Арлана, не на другом компьютере команды или компьютере жюри.
- В прежней браузерной проверке прошли реальные D1/D4/D5; остальные UI-сценарии и смысловую различимость всех финальных объяснений команда должна просмотреть на демонстрации.
- Не подтверждён способ доступа жюри к AI; локальный fallback не доказывает live AI. Ключи в Git не публиковать.
- «Сдать решение» на платформе не выполнено этим тестом и не заменяется push/merge.
