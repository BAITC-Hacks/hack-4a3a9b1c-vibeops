# Интерфейс Firebird

Владелец: Нурбол. Ветка: `feat/ui`. Контракт: раздел 5 FIREBIRD-CODEX-PACK.md, v1.

## Актуальный статус после исправлений backend

23.09 около 14:30 UTC+5: в UI-ветку включён main 4625919 с исправленным PR #6. Сборка с typecheck, 32 backend/AI-теста и 35 UI-тестов прошли. Старые блокеры 501/Vendor[]/null-language/duration_hours, описанные ниже в истории, исправлены.

Независимая приёмка без AI-ключа: D1–D6 через production API на исходном CSV, по три повтора, все PASS; время 2–81 мс в этом локальном замере (не SLA и не live AI). Через настоящий API в Edge также пройдены все шесть кнопок; количество карточек 3/3/1/0/0/2, три исхода различимы. Порядок D1: HK-88430, HK-27222, HK-29829; D2: HK-44733, HK-27222, HK-44923. Desktop D1 и mobile D6 просмотрены, ошибок страницы и горизонтального скролла на 390 px нет. Подмен API не было, предупреждение fallback видно.

Дополнительно HTTP: hours:100 с language:null → 200 no_matches; hours:0 → 422 VALIDATION_ERROR; язык английский для D1 → 200 no_matches. Локальная повторная live-AI проверка не выполнялась; результаты живого AI интегратора приведены в разделе PR #6 ниже. Финальные clean-start, доступ жюри и сдача не подменяются этими проверками.

## Подключение к каркасу Никиты

- `client/index.html` — вход Vite; `client/src/main.tsx` — React entry.
- Frontend импортирует типы `Query`, `Card`, `Reason`, `RecommendResponse`, `ApiError` из `shared/contracts.ts` в корне. Экспорты сверены с опубликованным каркасом Арлана.
- Runtime-зависимости: `react`, `react-dom`. Build-зависимости: `vite`, `typescript`, типы React/React DOM. Дополнительных UI-библиотек нет. Общими package.json, lockfile и конфигурацией владеет Никита.
- Рекомендуемая настройка Vite: `root: 'client'`, dev proxy `/api` на backend, `build.outDir: '../dist/client'`. Express раздаёт этот каталог и index.html в production. Это требование интеграции, не утверждение о готовом общем запуске.
- TypeScript: DOM libs, `jsx: react-jsx`. Единственный импорт внешних типов — общий контракт.
- Запросы same-origin: `GET /api/options`, `POST /api/recommend`. CSV, фильтрация, ранжирование и ключи на клиенте отсутствуют.

## Реализованные состояния

Форма содержит все пять обязательных полей, опциональные язык/часы. Options приходят с сервера, категории глобальные. Дата ограничена окном сервера, бюджет допускает 0, пустые язык/часы уходят как null.

Три outcome различимы словами. Карточки отображаются в порядке API, максимум 3; цена обозначена «от», max_hours=null обозначен как неприменимость часов присутствия. Показаны synthetic/imputed, объяснение, источник и цитата. Нет бронирования или обещания окончательной цены.

При смене поля предыдущий запрос отменяется, результат очищается. Request sequence дополнительно защищает от устаревшего ответа. Submit заблокирован во время запроса; демо позволяют сменить запрос и отменить предыдущий. Timeout клиента 15 секунд — защита от зависшего соединения, не обещание выполнения ориентира DoD до 10 секунд.

Ошибка сервера/сети отображается отдельно от no_matches. Режимы fallback/mixed и warning видимы. Причины исключения показаны с пересекающимися счётчиками и отдельным числом исключённых профилей. Поимённая занятость показывается для даты результата.

Демо D1–D6 содержат только параметры запросов из пакета. Результаты всегда поступают от backend. Все строки из API выводятся средствами React как текст, без HTML-вставок.

## Проверки и ограничения

На 23 сентября 2026, 13:38 UTC+5, main на официальном remote всё ещё содержал только Initial commit `03f2077` и README; ветки feat/core не было. Общий каркас и backend не проверены.

Проверено изолированно, без изменения общих файлов:

- TypeScript strict: ошибок нет.
- 24 теста Vitest/Testing Library: пройдены. Точные запросы D1–D6, null-опции, глобальные категории, три исхода, fallback/mixed, маркировка данных, порядок карточек, безопасный вывод текста, валидация, сетевые/серверные ошибки, повтор options, отмена и поздний ответ, timeout, повреждённый API-ответ.
- Production Vite build: успешно (React 19, Vite 6.4.3, Vitest 3.2.7).
- Production bundle в Edge headless: три карточки, отсутствие ошибок браузера; ширины 1440, 390 и 320 px, без горизонтального скролла на мобильных. Скриншоты desktop/mobile просмотрены.

Это UI-проверки с тестовыми ответами API, а не подтверждение реального ranking, AI или полного запуска проекта. Изолированная среда, временный контракт, node_modules и скриншоты находятся в локальном tmp/ui-validation и не входят в поставку ветки.

### Актуальные команды UI-проверок

Зависимости и команда уже включены командой в main `bd5f129`. Команды выполнять из корня проекта, после остановки dev-сервера (Ctrl+C), чтобы Windows не блокировала esbuild:

```sh
npm ci
npm run typecheck
npm test
npm run test:ui
npm run build
```

Конфигурация тестов изолирована от общего Vite config. Tests импортируют реальный client/src/App.tsx. Fixtures в tests/ui используются только тестами и не включаются в клиентскую сборку.

Возможность live AI, реальный ranking и правильность причин исключения проверяются совместно с backend после первой интеграции. Фронтенд не исправляет и не пересортировывает выдачу сервера.

## Интеграция с опубликованным каркасом

В feat/ui объединён origin/main `66970d8` (каркас Арлана, PR #2). Конфликты касались только стартовых UI-заглушек: сохранены полноценный App, его entry и HTML. Общие package/lock/config, backend, каталог и AI-модуль не изменены относительно main.

После `npm ci` из общего lockfile успешно выполнены `npm run build` (включая оба TypeScript-проекта) и `npm test` — 17 штатных тестов. UI-тесты проверяются отдельно: общий npm test пока запускает только *.test.ts, не UI *.test.tsx. Владелец package.json должен добавить UI devDependencies и команду из раздела выше; по одному npm test нельзя утверждать, что UI-тесты запускались.

Проверен production UI в Edge с настоящим Express и исходным CSV, без подмен API: health сообщает 66 профилей, форма загружает 17 глобальных категорий; текущий POST /api/recommend возвращает 501 NOT_IMPLEMENTED и отображается явной ошибкой. Это подтверждает подключение UI к каркасу, но не работу ещё отсутствующего matching. После реализации Никитой нужно повторить D1–D6 и live AI сквозным сценарием.

## Подключение UI к main — 23.09, около 14:10 UTC+5

В интеграционной ветке codex/integrate-ui объединён feat/ui с main 249c861 (включая AI v3). Добавлены закреплённые devDependencies vitest 3.2.7, jsdom 26 и @testing-library/react 16 и команда npm run test:ui. Выполнены npm run build, npm test (21 проверка) и npm run test:ui (24 проверки), все успешно.

Backend Никиты 98b7300 отдельно проверен и НЕ включён: возвращает Vendor[] вместо RecommendResponse, language:null исключает все профили D1, duration_hours расходится с hours, отсутствует валидация. На main до исправления остаётся явный 501; UI корректно отображает ошибку. Полный подбор ещё не подтверждён.

## Приёмка настоящего API после подключения backend

Запустить приложение (`npm run dev` или после сборки `npm start`). В другом терминале из корня:

```sh
node tests/ui/acceptance.mjs
# При нестандартном локальном порте:
node tests/ui/acceptance.mjs http://127.0.0.1:3001
```

Runner использует только встроенный fetch Node, настоящий API и запросы D1–D6; карточки не подменяет. Он делает по три запроса на сценарий, проверяет outcome, допустимые ID, количество, бюджет/город/категорию, null-поля, причины занятости, согласованность AI-источников, неизменность порядка и каталога. Порядок четырёх eligible ID в контрольной таблице не считается заранее заданным ranking. Проверяется отличие D1 от D2. Время замеряется на стороне клиента до полного чтения ответа, ориентир 10 секунд. Вызовы могут расходовать AI-кредиты, если локальный сервер настроен на live AI.

Exit codes: 0 — автоматические проверки прошли; 1 — ошибка, расхождение или превышение 10 секунд; 2 — backend вернул 501 NOT_IMPLEMENTED, приёмка заблокирована. Состояние 501 никогда не считается успешным пустым результатом. Самопроверки runner на fixtures входят в npm run test:ui и не являются проверкой живого API.

### Контрольные сценарии

Все запросы: hours=null, language=null. Подробные параметры находятся в кнопках UI и tests/ui/acceptance-checks.mjs; источник — раздел 8 командного пакета.

| Сценарий | Ожидаемый результат | Что увидеть в интерфейсе |
|---|---|---|
| D1 Ведущий, Алматы, корпоратив, 10 октября, 1 млн ₸ | 10 в категории, 4 подходят, 3 карточки | Конкретные разные объяснения; Буллма HK-44733 занят |
| D2 те же условия, 11 октября | 10 в категории, 4 подходят, 3 карточки, состав отличается | Куррапика HK-88430 занят; объяснение изменения через календарь |
| D3 Флорист, Алматы, свадьба, 10 октября, 500 тыс. ₸ | 2 в категории, 1 подходит: HK-39372 | Одна карточка; HK-90001 занят; часы присутствия не применимы |
| D4 Декоратор, Астана, свадьба, 10 октября, 500 тыс. ₸ | no_category_in_city | Словами: категории в городе нет, не ошибка сети |
| D5 условия D1, бюджет 1 ₸ | no_matches, 10 выше бюджета | Словами: никто не проходит; причины, не пустой экран |
| D6 Банкетный зал, Алматы, свадьба, 14 ноября, 5 млн ₸ | 7 в категории, 2 подходят: HK-64395, HK-90011 | Две карточки, причины исключения других площадок |

### Ручные пункты — автоматические тесты их НЕ закрывают

- [ ] D1: убрать имена из объяснений, убедиться, что основания действительно различаются и относятся к своим профилям; 1–2 предложения, без общей рекламы. Проверить то же для D2/D6.
- [ ] Проверить D1–D6 в UI, не только API; D1/D2 должны наглядно показывать влияние даты.
- [ ] Записать порядок D1, перезапустить сервер и повторить: порядок тот же. Проверка на перемешанном каталоге — зона backend.
- [ ] Пройти UI клавиатурой; проверить поля/ошибки, отмену запроса при смене даты, мобильный экран, отсутствие горизонтального скролла.
- [ ] Отдельно пройти live AI и режим без ключа/недоступного провайдера. Fallback не выдаётся за live AI; не публиковать ключи в клиенте, README или скриншотах.
- [ ] Из чистой итоговой main: npm ci → все тесты → build → start → D1–D6. Открывать localhost, не client/index.html через file:// и не Live Server.
- [ ] Ответственный проверил доступ жюри без личного аккаунта участника и обновил итоговый README; команда может объяснить пайплайн.
- [ ] Ответственный отдельно выполнил «Сдать решение» на платформе. PR, push и зелёные тесты не заменяют сдачу.

Корневые README и package/lock уже обновлены интегратором; дополнительные несогласованные правки в них из UI-ветки не требуются. Незакрытые пункты после backend: итоговый clean-start, смысловая проверка объяснений, живой AI и сдача.

### Повторная проверка 23 сентября около 14:20 UTC+5

После обновления до main bd5f129: npm ci, npm run build (с typecheck), npm test (21), npm run test:ui (35, включая 11 самопроверок acceptance checker) прошли. Runner против настоящего production-каркаса на локальном порте 3017 получил 501 и сообщил BLOCKED, не PASS. Без запущенного сервера сообщает FAIL. Реальная приёмка D1–D6 пока не пройдена.

Проверен опубликованный backend PR #6, ветка codex/scaffold, commit 5680b19, без объединения и изменения чужого кода. Прямой вызов его recommend на исходном CSV воспроизвёл:

- Точный D1 с language:null возвращает [] вместо четырёх подходящих профилей до ограничения top-3.
- Тот же D1 при отсутствии language возвращает HK-88430, HK-77838, HK-27222, HK-29829 — проблема именно в обработке null.
- hours:100 при отсутствии language всё ещё возвращает эти четыре профиля с max_hours 6/8/10/6: функция проверяет duration_hours вместо hours.
- Возвращается Vendor[], а UI по общему контракту ожидает RecommendResponse: cards, query, outcome, summary, explanation, meta. В этой функции нет top-3/ранжирования и вызова AI; handler не валидирует Query.

Передача Никите: принять null как отсутствие опционального фильтра; использовать hours; добавить серверную валидацию; вернуть неизменённый общий контракт с тремя исходами, диагностикой и не более тремя отсортированными карточками; подключить explainSelection. Нельзя исправлять это снятием фильтра языка/часов на клиенте или подменой ответа в UI. Повторить проверку на новой версии PR, поскольку текущие наблюдения относятся строго к 5680b19.

## Интеграция backend PR #6

По отчёту интегратора, включённому в main: после исправления конфликтов и контракта проверены production UI: D1 — три реальные карточки и LLM-объяснения; D4 — категории нет в городе; D5 — никто не подходит. Все D1–D6 также проверены HTTP-тестами на исходном CSV; production API D2/D3/D6 вернул live LLM. 32 backend/AI + 24 UI теста прошли. Ранее описанные 501 и несовместимость Vendor[] относятся к предыдущему состоянию и исправлены.
