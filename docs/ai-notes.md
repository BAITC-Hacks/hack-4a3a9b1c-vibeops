# AI-объяснения Firebird

Владелец: Арлан. Ветка `feat/explanations`. Модуль не фильтрует, не ранжирует и не запускает HTTP-сервер.

## Подключение Никите

Нужна серверная зависимость `openai@7.23.0` (эта версия проверена). Общие `package.json`, lockfile и `shared/contracts.ts` меняет Никита. Типы импортируются из `shared/contracts.js` по контракту v1 FIREBIRD-CODEX-PACK; расширение `.js` рассчитано на TypeScript NodeNext/tsx.

```ts
import { explainSelection, fallbackExplanation } from './explanations/index.js';

const selected = selection.ranked.slice(0, 3);
const explanations = await explainSelection({
  query: selection.query,
  candidates: selected,
  dataset_sha256: catalog.sha256,
});
const byId = new Map(explanations.items.map(item => [item.id, item]));
// Карточки собирать в порядке selected, соединяя по vendor.id.
// При дополнительном восстановлении через fallbackExplanation обновить общий mode.
```

`explainSelection` принимает не более 3 уникальных выбранных профилей. Ошибка границы означает ошибку вызывающего кода. Для пустого массива вернёт `not_needed` без обращения к API. `fallbackExplanation(query, candidate)` — синхронная функция.

## Конфигурация и SDK

- `OPENAI_API_KEY` — серверный ключ; `OPENAI_MODEL` — явно заданная модель.
- Живой batch подтверждён на `gpt-4.1-mini` 23.09.2026 около 13:32 UTC+5.
- Окружение загружает общий entrypoint сервера (dotenv или Node `--env-file`), сам модуль `.env` не читает.
- Responses API, `text.format.type=json_schema`, strict schema `{items:[{id,quote}]}`, `store:false`, максимум 1200 выходных токенов.
- Один запрос на весь набор карточек, SDK `maxRetries:0`. Общий deadline 6000 мс, AbortController + Promise.race; задержку остального backend учитывать отдельно.
- Модель и ключ не зашиты в код; ключи не выводятся в диагностику. Системный текст отделён от недоверенных строк описания/запроса, инструменты выполнения не подключены.

Официальные источники: [Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs), [GPT-4.1 mini](https://developers.openai.com/api/docs/models/gpt-4.1-mini). JSON Schema гарантирует форму, а факты проверяет наш код.

## Свидетельства и резервный режим

Версия промпта `firebird-evidence-v3`. Код выделяет до 60 непрерывных предложений/клауз на профиль длиной 18–220 Unicode-символов. Для длинных списков учитываются маркеры •; текст без пунктуации делится на непрерывные фрагменты по границам слов. JSON Schema anyOf связывает каждый id со своим enum дословных фрагментов (плюс пустая строка). Модель выбирает релевантное свидетельство из этих вариантов; свободное переписывание исключено схемой, серверная валидация сохранена. Это улучшение после live проверки v1, где модель перефразировала один профиль; такие ответы корректно попадали в fallback. Проверка нормализует Unicode NFC и пробелы, сохраняя регистр, цифры, слова и пунктуацию. Чужие ID отбрасываются, повторяющиеся ID не используются, цитата из другого профиля не проходит. Пустая/невалидная/отсутствующая цитата приводит к извлечению кодом для этой карточки.

Первое предложение собирается из структурированных полей: формат, дата, стартовая цена и бюджет, заданные язык/часы. Второе — атрибутированная цитата. `max_hours=null` не превращается в обещание неограниченной работы. Отсутствие даты в busy_dates не подтверждает бронирование. Стартовая цена не гарантирует итоговую стоимость — общую заметку об этом показывает UI.

Fallback разбивает описание на предложения/клаузы, выбирает конкретный фрагмент по маркерам формата, языка, услуг и опыта. Общая похвала может дать `quote:null`; факты каталога останутся. Это лексическая эвристика, **не LLM**. Фильтры и ranking принадлежат backend.

Каждая карточка имеет `source:llm|fallback`; весь результат — `llm|mixed|fallback|not_needed`. При полном fallback `model:null`. Ошибки провайдера преобразуются в безопасные русские предупреждения: отдельно 429, 401/403, таймаут, неверный ответ. Причина из сырого exception не публикуется.

Кэш: до 200 полностью проверенных LLM-ответов, LRU, TTL 10 минут. Ключ включает canonical query, SHA-256 CSV, модель, версию промпта и выбранных кандидатов. Ошибки/mixed не кэшируются; восстановившийся провайдер можно вызвать снова. Ответы клонируются, потребитель не меняет кэш. Кэш не обеспечивает и не меняет детерминизм ранжирования.

## Фактические проверки

23.09.2026: изолированная сборка в `/tmp` против точной копии контракта из пакета, OpenAI SDK 7.23.0. `tsc --noEmit` и 14 тестов `tsx --test tests/explanations/*.test.ts` прошли. После добавления общего каркаса: npm run build и все 17 тестов прошли также в чистой копии. Модуль компилируется с опубликованным shared/contracts.ts; вызов из /api/recommend ещё должен подключить Никита.

Тесты: пустая выдача без API; отсутствие конфигурации; один batch и сохранение порядка; чужие/повторные ID и чужая цитата; изменённые факты/длина/несколько предложений; неверная схема; 429/auth/500 без утечки исключения; deadline и abort; кэш/TTL/смена dataset и модели/лимит 200; null hours; нарушение входного контракта. SDK проверен с подменённым HTTP transport: правильный endpoint/schema и отсутствие retries на 429. Это **unit/provider-stub**, не живой AI.

Ручная проверка D1 на исходном CSV (отдельно вычисленная выборка): HK-88430, HK-27222, HK-29829. Fallback-цитаты различимы без имён: корпоративы/деловые встречи; сотрудничество с перечисленными брендами; развлечения и танцы. Это содержимое профилей, не независимо подтверждённые факты. Смена даты и исключения — задача backend/UI.

Live D1 на v2: 3799 мс холодный запрос, все 3 source=llm, mode=llm, warning=null; повтор 0 мс после округления (менее 1 мс), cached=true. Реальные цитаты: корпоративы/деловые встречи; сотрудничество с перечисленными брендами; развлечения и танцы. Проверены принадлежность источнику и различимость без имён. Это один успешный замер модуля, не SLA и не скорость полного UI/API.

Осталось: подключение подбора и сквозная проверка UI/API; доступ жюри к серверному AI. Создание ключа и наличие кредитов не доказательство успешного вызова. Ограничения: проверка принадлежности цитаты не доказывает её смысловую релевантность или истинность; LLM-цитаты между холодными запусками могут различаться, порядок подрядчиков — нет.

Для жюри нужен настроенный серверный ключ через согласованный канал или демонстрационный сервер. Без ключа приложение сохраняет объяснения на реальных данных, но показывает fallback; это не подмена live AI.

## Повторяемая проверка AI на датасете (23.09.2026, после 14:00)

Добавлены tests/explanations/demo-cases.ts, dataset.test.ts и live-demo.ts. Варианты цитат всех 66 профилей проверены на принадлежность исходному описанию и ограничение длины. На реальных данных найдены и исправлены отсутствие вариантов у длинного описания без пунктуации (HK-26808) и отсутствие содержательного fallback у панорамной площадки (HK-64395). Контракт API, фильтры и код других участников не изменялись.

```bash
# Без обращения к API: честный fallback по реальным профилям
node --import tsx tests/explanations/live-demo.ts
# Платные запросы разрешены только явным флагом; .env загружается серверно
node --import tsx tests/explanations/live-demo.ts --live
```

Обычный npm test не запускает live-demo. Проверки typecheck и 21 тест прошли. В demo-cases заданы допустимые id из оригинального каталога; это НЕ результат selectVendors и НЕ тест порядка ranking, /api/recommend или UI. В D2 намеренно присутствует HK-44733 для второй даты. Ключ, HTTP headers и внутренние ошибки не печатаются.

| Сценарий модуля | Холодный запрос, мс | Режим | Повтор |
|---|---:|---|---|
| D1 | 2914 | llm | кэш, <1 мс |
| D2 | 2953 | llm | кэш, <1 мс |
| D3 | 2613 | llm | кэш, <1 мс |
| D4 | 0 | not_needed | без API |
| D5 | 0 | not_needed | без API |
| D6 | 1477 | llm | кэш, <1 мс |

Все 9 объяснений непустых выборок получили source=llm. Проверка принадлежности цитат пройдена; D1 различает деловые встречи, телевидение/масштабные события и танцы, D3 — цветочное оформление, D6 — панорамную локацию и банкетный зал/ресторан. D2 использует сведения об опыте, индивидуальном сценарии и двуязычии; предпочтение более конкретных форматных деталей остаётся предметом ручной оценки. Замеры единичные, не гарантия скорости под нагрузкой. D4/D5 проверяют только отсутствие вызова AI при пустом входе, не правильность фильтрации backend.


## Понятные объяснения v4 — 23.09, около 15:00 UTC+5

Первой идёт точная цитата с индивидуальной особенностью, второй — краткая связь с условиями запроса: формат, стартовая цена и бюджет, выбранные язык/часы, дата без отметки занятости. Дата показана как ДД.ММ.ГГГГ. Источник цитаты явно обозначен словами «В описании»; это сведения профиля, не независимо подтверждённые факты. Подтверждённое бронирование не обещается.

Варианты для модели и runtime-проверка теперь используют одинаковый лексический фильтр конкретности: общая реклама без признаков услуги/опыта исключается, в том числе «идеальный выбор для корпоративного мероприятия». Сам по себе номер/год больше не делает цитату конкретной. Если конкретного основания не найдено, fallback прямо сообщает об этом вместо рекламной фразы. Это эвристика, не гарантия понимания смысла: неполный словарь может пропустить полезную цитату или принять слабую. Различимость объяснений остаётся предметом ручной оценки.

Добавлены проверки рекламной цитаты от провайдера, честного отсутствия индивидуального основания, сохранения цены/даты/часов/языка и объяснений реальной top-3 выборки selectVendors для D1–D6 (в отличие от прежних заранее выбранных ID). Прошли 36 backend/AI-тестов, 35 UI-тестов и production-сборка.

Живой production API после сборки прошёл D1–D6 с явной проверкой mode=llm для непустых результатов: D1 3052 мс, D2 2186 мс, D3 1152 мс, D6 1979 мс; D4/D5 — not_needed. D1 различает специализацию на деловых мероприятиях, опыт телевидения/крупных событий и развлечения/танцы. D6 различает панорамную локацию и зал до 200 гостей с кейтерингом/парковкой. Это единичные замеры, не SLA; UI в этом прогоне проверен unit-тестами, не новым браузерным просмотром.

Также устранены два остатка слияния main 6ad2829: лишний идентификатор main в handler вызывал HTTP 500, пропущенный импорт Catalog ломал typecheck. Существующие HTTP-тесты обнаружили регрессию и после исправления прошли.


## Контракт альтернатив и сравнения — 23.09, после 15:15

По согласованной идее подготовлены shared/decision-support.ts, optional RecommendResponse.decision_support и server/explanations/decision-support.ts. describeAlternative форматирует два настоящих Selection, допускает только улучшение количества при смене одного поля и не обещает бронирования. buildComparison повторно использует проверенные цитаты текущих cards, сохраняя source/order; новых AI-вызовов нет.

Пройдены 41 backend/AI-тест (5 новых на альтернативы и сравнение), сборка; UI-набор из 35 тестов прошёл до расширения, клиентский код в этой ветке не менялся. Исходный main 5289994 отдельно прошёл живой API D1–D6 перед новой функцией. Спецификация, задания Никите/Нурболу и настоящие примеры — docs/decision-support/.

Статус: готова наша библиотека текстов и общий контракт. Поиск предложений и поле в реальном ответе API должен подключить Никита, интерфейс — Нурбол. Новая функция пока НЕ проверена сквозным сценарием и не объявляется готовой. describeAlternative сам не ищет ближайшую дату/минимальный бюджет; соответствующее обязательство находится в алгоритме SPEC.md.
