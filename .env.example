PORT=3000
DATASET_PATH=data/vendors.csv
# Optional for catalog and fallback; both required for live AI.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-6-sol
# Optional candidate after live access/latency checks: OPENAI_MODEL=gpt-6-astra.
# The v0.1.0 release live acceptance used gpt-6-sol; see docs/release-acceptance.md.
